import { SidebarMenu, SidebarMenuButton, SidebarMenuItem } from './ui/sidebar'

const TeamSwitcher = () => {
  return (
    <SidebarMenu>
        <SidebarMenuItem>
            <SidebarMenuButton>

            </SidebarMenuButton>
        </SidebarMenuItem>
    </SidebarMenu>
  )
}

export default TeamSwitcher